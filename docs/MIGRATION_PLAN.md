# ConvertZZ 2.0 遷移計畫與驗收清單

## 文件目的

本文件定義 ConvertZZ 2.0 的遷移範圍。

本文件也定義可重複執行的驗收條件。

所有必要項目通過後，遷移才算完成。

舊 WPF 專案會保留至最終驗收完成。

## 目標

- 版本提升為 `2.0.0`。
- 桌面介面遷移至 Vue 3、Element Plus 與 Tauri 2。
- 文字與檔案核心遷移至 Node.js sidecar。
- Windows x64 與 Linux x64 共用相同轉換核心。
- 新式分詞轉換成為預設方法。
- 舊版六欄字典保留為相容方法。
- APE 與 OGG 標籤成為正式支援功能。
- 發行包不要求使用者另行安裝 Node.js。
- GPL-3.0-only 授權保持不變。

## 完成定義

目前狀態以 2026-08-14 的工作目錄為準。

目前狀態不是正式發行簽核。

驗收狀態使用下列標記。

| 標記 | 定義 |
| --- | --- |
| `已通過` | 功能已實作，而且指定證據已產生。 |
| `待人工驗收` | 功能已實作，但仍缺少指定平台的人工證據。 |
| `部分完成` | 只有部分條件成立。 |
| `未完成` | 功能尚未實作，或尚無法開始驗收。 |

單元測試通過不等同於桌面整合驗收通過。

本機建置通過不等同於安裝包驗收通過。

每一項驗收證據必須記錄版本、平台與日期。

螢幕互動項目必須保留截圖或錄影。

檔案安全項目必須保留輸入、輸出與雜湊紀錄。

發行項目必須以乾淨環境產生證據。

任何必要項目失敗時，不得移除舊 WPF 專案。

## 目標架構

| 層級 | 責任 | 技術 |
| --- | --- | --- |
| 前端 | 畫面、預覽、確認與狀態呈現 | Vue 3.5、TypeScript、Vite、Element Plus 2.14 |
| 桌面層 | 視窗、托盤、快捷鍵、檔案選擇與程序管理 | Tauri 2.11、Rust |
| 核心層 | 文字、檔案、編碼、字典與音訊標籤 | Node.js 24 sidecar |
| 發行層 | sidecar 編譯、WASM 資源與安裝包 | pnpm、`@yao-pkg/pkg`、Tauri Bundler |

前端與 sidecar 使用長駐 NDJSON 通訊。

每個要求都包含識別碼、操作名稱與型別化資料。

每個回應都包含結果、警告、進度或結構化錯誤。

Sidecar 記錄只寫入標準錯誤串流。

前端不取得任意 Shell 執行權限。

檔案路徑只可來自選擇器或已驗證的工作項目。

## 遷移階段

### 第一階段：基線與相容資料

保留舊版原始碼與資料格式。

建立 `SettingsV2` 與舊設定匯入流程。

保留 `Dictionary.csv` 的 UTF-8 BOM 六欄格式。

本階段完成後，不得直接覆寫舊設定或舊字典。

### 第二階段：共用轉換核心

建立 Node.js sidecar 與型別化通訊協定。

實作新式分詞、舊版字典與 ZhConvert 三種引擎。

實作長文字切分與所有必要編碼。

本階段完成後，相同輸入應在 Windows 與 Linux 產生相同結果。

### 第三階段：桌面介面

建立 Vue 與 Element Plus 主介面。

建立快速轉換、檔案、剪貼簿、音訊、工具、字典、設定與關於頁面。

建立浮動球、托盤、全域快捷鍵與單一執行個體行為。

本階段完成後，使用者可在不開啟舊 WPF 程式的情況下完成主要操作。

### 第四階段：檔案與音訊安全

建立檔案與標籤預覽。

建立確認、衝突、暫存寫入、驗證與回復流程。

使用 `mp3tag.js` 處理 MP3。

使用 `taglib-wasm` 處理 APE、OGG、OGA 與 Opus。

本階段完成後，文字標籤以外的音訊內容不得改變。

### 第五階段：平台與發行

完成 Windows、X11 與 Wayland 的能力分流。

Linux 托盤使用 AppIndicator 執行函式庫。

AppIndicator 開發套件只存在於建置環境。

完成 Node.js sidecar 與 `taglib-wasi.wasm` 的離線封裝。

建立 Windows 與 Linux 發行工作流程。

本階段完成後，乾淨環境可直接安裝及執行。

### 第六階段：最終驗收與舊專案退場

執行本文件所有必要驗收項目。

整理失敗紀錄與平台差異。

所有必要項目通過後，才可移除舊 WPF 專案。

## 實作決策與已知差異

- 新式引擎只使用 `novel-segment` 的分詞與 `ZhtSynonymOptimizer`。
- 新式引擎只使用 `cjk-conv` 完成字形轉換。
- 專案不維護額外的硬編碼語意取代清單。
- Linux sidecar 會以 target triple 檔名建立。
- Linux 發行包會把 sidecar 作為 gzip 資源放入應用程式目錄。
- 啟動時會驗證 SHA-256，再安全解壓到應用程式快取目錄。
- 此配置可避免 AppImage 工具改寫 `@yao-pkg/pkg` 的內嵌資料。
- Windows 仍使用 Tauri `externalBin` sidecar。
- Linux 的 Tauri 托盤不提供左鍵事件。
- Linux 使用者需從托盤選單開啟主視窗。
- 舊版 `Encoding.Default` 會隨 Windows 系統碼頁改變。
- 新版編碼工具改用明確指定的編碼。
- 舊命令列批次改為先顯示預覽並要求確認。
- 此變更保留參數相容性，但刻意不保留無確認寫入行為。

## 明確驗收項目

### A. 專案與建置基線

| 編號 | 驗收條件 | 判定方式 | 必要證據 | 目前狀態 |
| --- | --- | --- | --- | --- |
| A-01 | `package.json` 的版本為 `2.0.0`。 | 檢查套件中繼資料。 | 檔案差異或 CI 紀錄。 | 已通過 |
| A-02 | 專案使用 pnpm 鎖定相依。 | 執行 `pnpm install --frozen-lockfile`。 | 指令成功紀錄。 | 已通過 |
| A-03 | 前端型別檢查成功。 | 執行 `pnpm run typecheck`。 | CI 或本機成功紀錄。 | 已通過 |
| A-04 | 前端正式建置成功。 | 執行 `pnpm run build:web`。 | CI 或本機成功紀錄。 | 已通過 |
| A-05 | Rust 格式檢查成功。 | 執行 `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`。 | CI 或本機成功紀錄。 | 已通過 |
| A-06 | Tauri 應用程式可完成原生編譯。 | 執行 `pnpm tauri build --debug --no-bundle --ci`。 | 建置成功紀錄。 | 已通過 |
| A-07 | 授權仍為 GPL-3.0-only。 | 檢查套件中繼資料與授權檔。 | 檔案差異。 | 已通過 |
| A-08 | 第三方套件與 WASM 授權聲明包含於原始碼及發行資料。 | 檢查聲明檔與安裝包內容。 | 聲明檔與安裝包清單。 | 已通過 |

### B. Sidecar 與通訊協定

| 編號 | 驗收條件 | 判定方式 | 必要證據 | 目前狀態 |
| --- | --- | --- | --- | --- |
| B-01 | Sidecar 使用 Node.js 24 建立平台執行檔。 | 執行 `pnpm run sidecar:build`。 | 可執行檔與建置紀錄。 | 已通過 |
| B-02 | Sidecar 名稱包含 Tauri target triple。 | 檢查 `src-tauri/binaries` 產物。 | 產物名稱清單。 | 已通過 |
| B-03 | 未安裝 Node.js 時仍可啟動 sidecar。 | 在乾淨環境執行封裝後的程式。 | 環境清單與健康檢查回應。 | 部分完成 |
| B-04 | NDJSON 要求包含唯一識別碼、操作與資料。 | 傳送兩筆連續要求。 | 原始要求與回應紀錄。 | 已通過 |
| B-05 | 回應可傳回結果、警告與結構化錯誤。 | 執行成功與故意失敗的要求。 | 原始 NDJSON 紀錄。 | 已通過 |
| B-06 | 長作業會傳回可見進度。 | 執行大型檔案或批次標籤作業。 | 至少兩次中間進度與最終結果。 | 已通過 |
| B-07 | Sidecar 記錄不會混入標準輸出協定。 | 捕捉 stdout 與 stderr。 | 分流紀錄。 | 已通過 |
| B-08 | Tauri 只封裝固定 sidecar 命令。 | 檢查 capability 與 Rust 呼叫。 | 設定檔差異。 | 已通過 |
| B-09 | 前端無法執行任意 Shell 命令。 | 檢查 capability 與前端呼叫。 | 權限清單。 | 已通過 |

### C. 文字轉換引擎

| 編號 | 驗收條件 | 判定方式 | 必要證據 | 目前狀態 |
| --- | --- | --- | --- | --- |
| C-01 | 預設引擎為 `segmented`。 | 清除設定後啟動程式。 | 畫面截圖與設定內容。 | 已通過 |
| C-02 | 新式引擎使用 `novel-segment` 與 `cjk-conv`。 | 檢查鎖定版本與執行路徑。 | 相依清單與測試紀錄。 | 已通過 |
| C-03 | 簡轉繁會進行分詞、同義詞最佳化與地區字形轉換。 | 執行黃金測試。 | 測試結果。 | 已通過 |
| C-04 | 繁轉簡會先分詞再進行字形轉換。 | 執行黃金測試。 | 測試結果。 | 已通過 |
| C-05 | 「裡面」、「皇后」、「頭髮」與「開發」符合預期。 | 執行四組雙向黃金測試。 | 測試案例與結果。 | 已通過 |
| C-06 | 空白、換行、標點與未知片段保持原位。 | 比較輸入與輸出位置。 | 測試案例與結果。 | 已通過 |
| C-07 | 長文字會在安全邊界分段。 | 轉換超過單段上限的文字。 | 分段測試結果。 | 已通過 |
| C-08 | 舊方法可讀取六欄 `Dictionary.csv`。 | 載入含 BOM 的舊字典。 | 測試結果。 | 已通過 |
| C-09 | 舊方法保留啟用欄位、優先權與長詞優先。 | 執行衝突詞條測試。 | 測試結果。 | 已通過 |
| C-10 | 優先權 `9999` 會保護指定詞。 | 執行保護詞測試。 | 測試結果。 | 已通過 |
| C-11 | 舊方法未命中字元交由 `cjk-conv`。 | 使用不在字典內的字元。 | 測試結果。 | 已通過 |
| C-12 | ZhConvert 呼叫官方 `/convert` 端點。 | 使用模擬伺服器核對請求。 | 測試紀錄。 | 已通過 |
| C-13 | `/service-info` 快取二十四小時。 | 連續執行兩次轉換。 | 模擬伺服器請求次數。 | 已通過 |
| C-14 | ZhConvert 依服務大小限制切分文字。 | 回傳小型限制後轉換長文字。 | 請求分段紀錄。 | 已通過 |
| C-15 | ZhConvert 網路失敗時不切換引擎。 | 模擬連線失敗。 | 結構化錯誤紀錄。 | 已通過 |
| C-16 | ZhConvert 畫面顯示署名與商業使用提醒。 | 開啟 ZhConvert 選項。 | 畫面截圖。 | 已通過 |

### D. 編碼、文字工具與相容資料

| 編號 | 驗收條件 | 判定方式 | 必要證據 | 目前狀態 |
| --- | --- | --- | --- | --- |
| D-01 | UTF-8 與 UTF-16 可正確讀寫。 | 執行編碼往返測試。 | 測試結果。 | 已通過 |
| D-02 | Big5、GBK 與 Shift-JIS 可正確讀寫。 | 執行編碼往返測試。 | 測試結果。 | 已通過 |
| D-03 | EUC-JP、ISO-2022-JP 與 HZ-GB-2312 可正確讀寫。 | 執行編碼往返測試。 | 測試結果。 | 已通過 |
| D-04 | HTML 十進位與十六進位工具可雙向處理。 | 執行工具測試與畫面操作。 | 測試結果與截圖。 | 已通過 |
| D-05 | 全形與半形工具可雙向處理。 | 執行工具測試與畫面操作。 | 測試結果與截圖。 | 已通過 |
| D-06 | Unicode 跳脫工具保留舊版能力。 | 執行編碼與解碼案例。 | 測試結果與截圖。 | 已通過 |
| D-07 | 首次匯入 `ConvertZZ.json` 前會詢問使用者。 | 使用含舊設定的乾淨資料目錄啟動。 | 操作錄影。 | 待人工驗收 |
| D-08 | 同意匯入後會先建立不覆蓋的備份。 | 執行兩次匯入。 | 兩個備份檔名與內容雜湊。 | 已通過 |
| D-09 | 備份失敗時不匯入也不覆寫。 | 使用唯讀目錄執行匯入。 | 錯誤畫面與檔案雜湊。 | 部分完成 |
| D-10 | 所有已匯入的 `SettingsV2` 欄位會影響實際行為。 | 逐欄變更並重啟驗證。 | 設定驗收表。 | 部分完成 |
| D-11 | 儲存舊字典前會詢問使用者。 | 修改一筆詞條後按下儲存。 | 操作錄影。 | 待人工驗收 |
| D-12 | 同意儲存後會先建立不覆蓋的時間戳備份。 | 連續儲存兩次。 | 備份檔名與內容雜湊。 | 已通過 |

### E. 檔案與檔名安全

| 編號 | 驗收條件 | 判定方式 | 必要證據 | 目前狀態 |
| --- | --- | --- | --- | --- |
| E-01 | 檔案內容作業會先顯示來源、輸出、編碼與差異。 | 建立檔案轉換計畫。 | 預覽截圖。 | 待人工驗收 |
| E-02 | 檔名作業會先顯示來源與輸出。 | 建立批次重新命名計畫。 | 預覽截圖。 | 待人工驗收 |
| E-03 | 未確認前不會寫入或重新命名。 | 在預覽畫面取消。 | 前後檔案雜湊與目錄清單。 | 已通過 |
| E-04 | 衝突策略預設為略過。 | 建立同名輸出。 | 結果與原檔雜湊。 | 已通過 |
| E-05 | 覆寫需要額外確認。 | 選取覆寫策略。 | 操作錄影。 | 待人工驗收 |
| E-06 | 內容寫入使用同目錄暫存檔。 | 監看作業目錄。 | 檔案事件紀錄。 | 已通過 |
| E-07 | 驗證成功後才會原子取代。 | 模擬寫入成功與驗證失敗。 | 測試結果與檔案雜湊。 | 已通過 |
| E-08 | 檔名使用兩階段重新命名。 | 建立名稱互換案例。 | 測試結果與目錄清單。 | 已通過 |
| E-09 | 中途失敗時會回復原始名稱。 | 在第二階段注入失敗。 | 測試結果與目錄清單。 | 已通過 |
| E-10 | 遞迴處理不跟隨符號連結。 | 建立指向外部目錄的連結。 | 測試結果與外部目錄雜湊。 | 已通過 |
| E-11 | 唯讀檔案會回報可辨識錯誤。 | 對唯讀檔案執行寫入。 | 結構化錯誤紀錄。 | 已通過 |

### F. 音訊標籤

| 編號 | 驗收條件 | 判定方式 | 必要證據 | 目前狀態 |
| --- | --- | --- | --- | --- |
| F-01 | MP3 使用 `mp3tag.js` 讀寫 ID3v1 與 ID3v2。 | 掃描並更新 MP3 樣本。 | 測試結果與標籤清單。 | 已通過 |
| F-02 | ID3v1 可修復 Big5 與 GBK 文字。 | 使用兩種損壞標籤樣本。 | 修復前後欄位值。 | 已通過 |
| F-03 | ID3v2.3、ID3v2.4 與文字編碼選項可用。 | 逐一寫入並重新讀取。 | 標籤版本與內容紀錄。 | 部分完成 |
| F-04 | APE 使用 `taglib-wasm@2.0.0` 讀寫 APEv2。 | 掃描並更新 APE 樣本。 | 測試結果與標籤清單。 | 已通過 |
| F-05 | OGG 與 OGA 使用 Vorbis Comment。 | 掃描並更新兩種樣本。 | 測試結果與標籤清單。 | 已通過 |
| F-06 | Opus 使用 Vorbis Comment。 | 掃描並更新 Opus 樣本。 | 測試結果與標籤清單。 | 已通過 |
| F-07 | APEv2 與 Vorbis Comment 不顯示無效編碼選項。 | 開啟 APE、OGG 與 Opus 預覽。 | 畫面截圖。 | 待人工驗收 |
| F-08 | 所有可轉換字串欄位都會顯示。 | 使用標準、自訂與多值欄位樣本。 | 預覽截圖與原始標籤清單。 | 待人工驗收 |
| F-09 | 多值欄位會逐值轉換。 | 使用含兩個以上值的欄位。 | 轉換前後欄位值。 | 已通過 |
| F-10 | 未選欄位保持不變。 | 取消一個欄位後執行。 | 前後欄位雜湊。 | 已通過 |
| F-11 | 未知欄位保持不變。 | 使用自訂未知欄位樣本。 | 前後欄位值。 | 已通過 |
| F-12 | 二進位欄位與封面圖片保持不變。 | 更新含封面的樣本。 | 前後二進位雜湊。 | 已通過 |
| F-13 | 音訊內容不會重新編碼。 | 比較轉換前後的解碼後音訊雜湊。 | 雜湊紀錄。 | 已通過 |
| F-14 | 損毀與截斷檔案回報可辨識錯誤。 | 掃描兩種錯誤樣本。 | 結構化錯誤紀錄。 | 已通過 |
| F-15 | `taglib-wasi.wasm` 已包含於安裝包。 | 在離線環境掃描 APE 與 OGG。 | 安裝包清單與操作錄影。 | 已通過 |

### G. 桌面介面與作業系統整合

| 編號 | 驗收條件 | 判定方式 | 必要證據 | 目前狀態 |
| --- | --- | --- | --- | --- |
| G-01 | 主視窗含八個計畫頁面。 | 逐頁開啟並檢查。 | 頁面截圖組。 | 已通過 |
| G-02 | 浮動球使用獨立透明視窗。 | 顯示浮動球。 | Windows 與 Linux 截圖。 | 待人工驗收 |
| G-03 | 浮動球可用滑鼠拖動。 | 拖動後關閉並重啟。 | 操作錄影。 | 待人工驗收 |
| G-04 | 浮動球位置會在重啟後復原。 | 記錄拖動前後座標。 | 操作錄影與設定內容。 | 待人工驗收 |
| G-05 | 浮動球圖示沒有白底外框。 | 在深色與淺色桌面檢查。 | 兩張截圖。 | 待人工驗收 |
| G-06 | 浮動球顯示一大一小的 Z。 | 目視檢查圖示。 | 截圖。 | 已通過 |
| G-07 | 系統托盤會顯示應用程式圖示。 | 啟動後檢查通知區。 | Windows 與 Linux 截圖。 | 待人工驗收 |
| G-08 | Windows 托盤左鍵會顯示主視窗。Linux 會提供等效的開啟選單。 | 隱藏主視窗後操作托盤。 | Windows 與 Linux 操作錄影。 | 待人工驗收 |
| G-09 | 托盤右鍵會顯示選單。 | 右鍵點擊托盤。 | 操作錄影。 | 待人工驗收 |
| G-10 | 關閉主視窗時程式會留在托盤。 | 點擊主視窗關閉按鈕。 | 操作錄影與程序清單。 | 待人工驗收 |
| G-11 | 剪貼簿讀寫不會出現 capability 錯誤。 | 執行浮動球左右鍵轉換。 | 操作錄影與錯誤紀錄。 | 待人工驗收 |
| G-12 | 快捷鍵、托盤與浮動球呼叫相同動作路由。 | 對相同剪貼簿文字執行三種入口。 | 三次輸出比對。 | 待人工驗收 |
| G-13 | 第二個程序會把參數交給既有視窗。 | 連續啟動兩次程式。 | 操作錄影與程序清單。 | 待人工驗收 |
| G-14 | 問題回報會開啟 GitHub Issues。 | 點擊問題回報。 | 瀏覽器網址截圖。 | 已通過 |
| G-15 | 更新檢查會開啟 GitHub Releases。 | 點擊更新檢查。 | 瀏覽器網址截圖。 | 已通過 |

### H. 舊版命令列相容

| 編號 | 驗收條件 | 判定方式 | 必要證據 | 目前狀態 |
| --- | --- | --- | --- | --- |
| H-01 | `/e:l`、`/e:f` 與 `/e:n` 對應三種引擎。 | 解析三組參數。 | 測試結果。 | 已通過 |
| H-02 | `/audio` 會依副檔名辨識 ID3、APEv2 與 Vorbis Comment。 | 傳入四種音訊檔案。 | 預覽結果。 | 已通過 |
| H-03 | `/file` 會建立檔案預覽。 | 使用命令列開啟文字檔。 | 畫面截圖。 | 待人工驗收 |
| H-04 | 舊版方向、詞彙修正與編碼參數保持相容。 | 執行參數矩陣測試。 | 測試結果。 | 已通過 |
| H-05 | 舊版第二輸出檔與萬用字元語意保持相容。 | 執行代表性檔名案例。 | 測試結果與輸出清單。 | 已通過 |
| H-06 | Windows SendTo 捷徑只出現在 Windows。 | 在 Windows 啟用與停用。 | 捷徑內容與操作錄影。 | 待人工驗收 |

### I. 平台差異

| 編號 | 驗收條件 | 判定方式 | 必要證據 | 目前狀態 |
| --- | --- | --- | --- | --- |
| I-01 | Windows 可使用全域快捷鍵與自動複製貼上。 | 在 Windows x64 實機操作。 | 操作錄影。 | 待人工驗收 |
| I-02 | Linux X11 可使用全域快捷鍵與自動複製貼上。 | 在 X11 實機操作。 | 操作錄影。 | 待人工驗收 |
| I-03 | Wayland 不會嘗試鍵盤注入。 | 在 Wayland 執行剪貼簿動作。 | 日誌與操作錄影。 | 待人工驗收 |
| I-04 | Wayland 全域快捷鍵會顯示為停用或受限。 | 在 Wayland 開啟設定與關於頁。 | 畫面截圖。 | 待人工驗收 |
| I-05 | Wayland 浮動球置頂限制會清楚顯示。 | 在 Wayland 開啟平台差異。 | 畫面截圖。 | 待人工驗收 |
| I-06 | Linux 缺少 AppIndicator 桌面支援時主視窗仍可使用。 | 在不顯示 StatusNotifier 圖示的桌面啟動。 | 操作錄影與日誌。 | 待人工驗收 |
| I-07 | Linux 缺少 Secret Service 時只保留工作階段 API 金鑰。 | 關閉 Secret Service 後重啟兩次。 | 設定與日誌紀錄。 | 待人工驗收 |
| I-08 | README 差異表與實際能力一致。 | 完成三平台矩陣後逐列比對。 | 簽核後的差異表。 | 待人工驗收 |
| I-09 | Linux 使用者不需要安裝 AppIndicator 開發套件。 | 在沒有 `libayatana-appindicator3-dev` 的乾淨環境執行安裝包。 | 套件清單與操作錄影。 | 待人工驗收 |

### J. 測試與發行

| 編號 | 驗收條件 | 判定方式 | 必要證據 | 目前狀態 |
| --- | --- | --- | --- | --- |
| J-01 | `pnpm run check` 全部必要測試通過。 | 在含完整音訊樣本的 CI 執行測試。 | CI 連結。 | 部分完成 |
| J-02 | 音訊測試包含 MP3、APE、OGG 與 Opus。 | 檢查測試樣本與完整報告。 | 無略過項目的測試報告。 | 已通過 |
| J-03 | ZhConvert 測試不連線至正式服務。 | 檢查測試網路目標。 | 模擬伺服器紀錄。 | 已通過 |
| J-04 | Windows x64 產生 NSIS 與 MSI。 | 執行 Release 工作流程。 | 兩個可下載產物。 | 待人工驗收 |
| J-05 | Linux x64 產生 AppImage、DEB 與 RPM。 | 在 Ubuntu 22.04 執行工作流程。 | 三個可下載產物。 | 部分完成 |
| J-06 | 每個發行檔都有 SHA-256。 | 對照產物與校驗檔。 | 校驗成功紀錄。 | 部分完成 |
| J-07 | Linux 執行相依不含 Node.js、`*-dev` 與其他建置套件。 | 檢查 DEB、RPM 與相依報告。 | 相依報告。 | 已通過 |
| J-08 | Windows 乾淨環境不需 Node.js 即可安裝與執行。 | 使用全新 Windows x64 虛擬機。 | 環境清單與操作錄影。 | 待人工驗收 |
| J-09 | Linux 乾淨環境不需 Node.js 即可安裝與執行。 | 使用全新 Ubuntu 22.04 虛擬機。 | 環境清單與操作錄影。 | 部分完成 |
| J-10 | AppImage 不要求使用者安裝開發套件。 | 在未安裝 `*-dev` 套件的環境執行。 | 套件清單與操作錄影。 | 待人工驗收 |
| J-11 | DEB 與 RPM 會由套件管理器補齊執行函式庫。 | 使用 APT 與 DNF 安裝。 | 安裝日誌。 | 待人工驗收 |
| J-12 | 發行工作流程建立草稿 Release。 | 推送測試標籤或手動執行。 | 草稿 Release 連結。 | 待人工驗收 |
| J-13 | 發行包不會在執行時下載 WASM。 | 離線執行 APE 與 OGG 掃描。 | 網路監看與操作錄影。 | 部分完成 |
| J-14 | 程式碼簽章缺失會在第一版說明。 | 檢查 Release 說明。 | Release 內容。 | 待人工驗收 |

### K. 最終退場閘門

| 編號 | 驗收條件 | 判定方式 | 必要證據 | 目前狀態 |
| --- | --- | --- | --- | --- |
| K-01 | A 至 J 的必要項目全部為 `已通過`。 | 檢查本文件狀態。 | 完整驗收紀錄。 | 未完成 |
| K-02 | 所有已知平台限制已寫入 README 差異表。 | 比對失敗與限制紀錄。 | README 差異。 | 部分完成 |
| K-03 | Windows x64 驗收人完成簽核。 | 填寫簽核紀錄。 | 姓名、版本與日期。 | 未完成 |
| K-04 | Linux X11 驗收人完成簽核。 | 填寫簽核紀錄。 | 姓名、版本與日期。 | 未完成 |
| K-05 | Linux Wayland 驗收人完成簽核。 | 填寫簽核紀錄。 | 姓名、版本與日期。 | 未完成 |
| K-06 | 舊 WPF 專案已建立可回復的最後版本標記。 | 建立版本標籤並驗證來源。 | 標籤與來源雜湊。 | 未完成 |
| K-07 | 舊 WPF 專案只在 K-01 至 K-06 通過後移除。 | 檢查移除變更。 | 變更審查紀錄。 | 未完成 |

## 2026-08-14 Linux X11 本機驗收

- 原始碼版本：`33b5009`。
- 平台：Linux Mint 22.3、X11、x86_64。
- `pnpm run check` 通過：93 項通過，4 項音訊整合因本機沒有 `ffmpeg` 而略過。
- 之後改以開發相依 `ffmpeg-static` 與 `tests/fixtures` 跑完整音訊整合。MP3、APE、OGG、OGA、Opus 寫入往返均通過。該 ffmpeg 不打進發行包。
- `cargo fmt --check` 通過。
- Sidecar `convertzz-sidecar-x86_64-unknown-linux-gnu` 健康檢查回傳 Node.js `24.18.1`。標準錯誤串流為空。
- `convert.preview`：「里面皇后头发开发」→「裡面皇后頭髮開發」。
- `utility.convert`：HTML 十進位／十六進位、全形／半形、Unicode 跳脫案例通過。
- 離線掃描 TagLib `mac-399.ape` 與 `test.ogg`：格式分別為 `ape`、`ogg`；OGG 多值欄位 `UNUSUALTAG` 保留兩個值。
- AppImage、DEB、RPM 皆含 `licenses/THIRD_PARTY_NOTICES.md`、`taglib-wasm-LICENSE.txt`、`taglib-COPYING.LGPL.txt`。
- Linux 發行檔 SHA-256：
  - AppImage `5d82b262b9743d709a50fb98d96505a78514a758e0ae2d6563410aa6ee15c8a8`
  - DEB `ac8e689209b717484cfb30b714e3889389749d85cada8f0f344d3f6122832e7f`
  - RPM `c5325616e48e409ddc37d8054fe5e112fc4788ec24a5926261688dc824778c4a`
- 啟動正式建置的 `ConvertZZ 2.0`：側欄八頁可開啟。設定預設引擎為「新式分詞」。ZhConvert 區塊顯示「使用此服務時必須遵守繁化姬的署名與商業使用條款。」浮動球為獨立視窗，圖示為一大一小的 Z。
- 音訊整合測試改由專案內 `ffmpeg-static` 執行，不再依賴系統 ffmpeg。
- 關於頁的「回報問題」「檢查更新」已由驗收人確認會開啟 GitHub Issues 與 Releases。
- 本次尚未取代 Windows、Wayland、乾淨虛擬機與發行工作流程驗收。

## 2026-08-14 Linux AppImage 本機驗證

- `pnpm tauri build --bundles appimage --ci` 成功。
- 產物為 `ConvertZZ_2.0.0_amd64.AppImage`。
- AppImage SHA-256 為 `5d82b262b9743d709a50fb98d96505a78514a758e0ae2d6563410aa6ee15c8a8`。
- 驗證在 Linux 網路命名空間內執行。
- 封裝後 sidecar 使用 Node.js `24.18.1`。
- 健康檢查通過。
- 「里面开发头发」轉換為「裡面開發頭髮」。
- 離線 APE 與 OGG 掃描通過。
- 封裝內 gzip 資源沒有執行權限。
- 解壓後 sidecar SHA-256 與封裝內校驗值相同。
- 本次尚未取代 Ubuntu 22.04 乾淨虛擬機驗收。
- 本次尚未取代 Windows、X11 與 Wayland 人工驗收。

## 2026-08-14 剩餘項目本機驗收

- 原始碼版本：`6828ff0` 加上本輪測試補強。
- 平台：Linux Mint 22.3、X11、x86_64。
- `pnpm run check` 通過：typecheck、110 項測試、前端正式建置。音訊整合不再略過。尚無 CI 連結，故 J-01 仍為部分完成。
- 新增／補強測試後再跑 `sidecar/src/audio/service.test.ts`、`migrate.test.ts`、`cli.test.ts`：27 項通過。
- D-08：連續兩次 `backupLegacySettings` 產生不同時間戳檔名，內容與來源相同。
- D-09：唯讀目錄下備份拋錯，來源 `ConvertZZ.json` 不變且沒有備份檔。匯入失敗時的對話畫面尚未錄影。
- E-03：取消計畫後 `files.apply` 回報 `PLAN_NOT_FOUND`，來源檔未改。
- E-06：寫入使用來源同目錄 `.convertzz-*` 暫存檔；覆寫與驗證失敗測試皆確認暫存會清除或不會取代原檔。
- F-02：ID3v1 以 Big5 與 GBK 寫入「裡面」，再用對應來源編碼掃描，標題皆讀回「裡面」。
- F-03：畫面與計畫可選 ID3v2.3／2.4 與文字編碼。以現有 `mp3tag.js` 寫入後，來源為 2.4 的樣本標頭仍為 2.4，故版本往返尚未視為通過。
- F-09：OGG `artist` 兩值「头发」「皇后」轉換後為「頭髮」「皇后」。
- F-15：本機 AppImage 與 DEB 皆含 `usr/lib/ConvertZZ/taglib-wasi.wasm`。先前離線 AppImage 驗證已掃描 APE／OGG。
- H-02：`/audio` 可帶入 mp3／ape／ogg／opus；`audio.scan` 依副檔名分別回報這四種格式。
- J-07：DEB `Depends` 為 `libayatana-appindicator3-1, libwebkit2gtk-4.1-0, libgtk-3-0`，不含 Node.js 或 `*-dev`。
- 下列項目仍缺指定平台的人工證據，維持待人工驗收：D-07、D-11、E-01、E-02、E-05、F-07、F-08、G-02 至 G-13（已通過者除外）、H-03、H-06、I-01 至 I-09、J-04、J-08、J-10 至 J-12、J-14。
- Windows、Wayland、乾淨虛擬機與發行工作流程簽核仍未開始。K 閘門未通過。

## 驗收紀錄格式

每次驗收使用下列表格。

| 欄位 | 內容 |
| --- | --- |
| 驗收編號 | 例如 `F-13`。 |
| 應用程式版本 | 例如 `2.0.0-rc.1`。 |
| 原始碼版本 | Git commit SHA。 |
| 平台 | Windows、Linux X11 或 Linux Wayland。 |
| 環境 | 作業系統版本、桌面環境與架構。 |
| 執行日期 | 使用 ISO 8601 日期。 |
| 執行者 | 驗收人姓名或帳號。 |
| 結果 | 通過或失敗。 |
| 證據 | CI 連結、檔案、截圖、錄影或雜湊。 |
| 備註 | 限制、重現步驟或後續工作。 |

## 最終簽核

| 平台 | 版本 | 結果 | 驗收人 | 日期 | 證據 |
| --- | --- | --- | --- | --- | --- |
| Windows x64 |  |  |  |  |  |
| Linux x64 X11 |  |  |  |  |  |
| Linux x64 Wayland |  |  |  |  |  |

三個平台完成簽核前，ConvertZZ 2.0 不視為完成遷移。
