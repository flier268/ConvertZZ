# ConvertZZ 2.0 遷移：已完成項目封存

本文件封存已通過的驗收項目。

進行中與待確認項目見 [MIGRATION_PLAN.md](MIGRATION_PLAN.md)。

狀態標記定義與完成定義見主計畫。本封存內項目狀態均為 `已通過`。

截圖與操作錄影留在倉庫外（預設 `~/Desktop/ConvertZZ-acceptance/`）；本文件只保留項目狀態與摘要。

---

## 已通過驗收項目

### A. 專案與建置基線

| 編號 | 驗收條件 | 判定方式 | 必要證據 | 目前狀態 |
| --- | --- | --- | --- | --- |
| A-01 | `package.json` 的版本為 `2.0.0`。 | 檢查套件中繼資料。 | 檔案差異或 CI 紀錄。 | 已通過 |
| A-02 | 專案使用 pnpm 鎖定相依。 | 執行 `pnpm install --frozen-lockfile`。 | 指令成功紀錄。 | 已通過 |
| A-03 | 前端型別檢查成功。 | 執行 `pnpm run typecheck`。 | CI 或本機成功紀錄。 | 已通過 |
| A-04 | 前端正式建置成功。 | 執行 `pnpm run build:web`。 | CI 或本機成功紀錄。 | 已通過 |
| A-05 | Rust 格式檢查成功。 | 執行 `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`。 | CI 或本機成功紀錄。 | 已通過 |
| A-06 | Tauri 應用程式可完成原生編譯。 | 執行 `pnpm tauri build --debug --no-bundle --ci`。 | 建置成功紀錄。 | 已通過 |
| A-07 | 授權仍為 GPL-3.0-only。 | 檢查套件中繼資料與授權檔。 | 檔案差異。 | 已通過 |
| A-08 | 第三方套件與 WASM 授權聲明包含於原始碼及發行資料。 | 檢查聲明檔與安裝包內容。 | 聲明檔與安裝包清單。 | 已通過 |

### B. 核心與通訊協定

| 編號 | 驗收條件 | 判定方式 | 必要證據 | 目前狀態 |
| --- | --- | --- | --- | --- |
| B-01 | 轉換核心與桌面層同程序，不另外封裝外部 Node.js 程序。 | 檢查 `package.json`、`tauri.conf.json` 與安裝包資源。 | 設定檔與安裝包清單。 | 已通過 |
| B-02 | 前端透過 `core_request` 呼叫型別化操作。 | 檢查 `shared/contracts.ts` 與 Rust dispatch。 | 契約與呼叫路徑。 | 已通過 |
| B-03 | 未安裝 Node.js 時仍可啟動已封裝程式。 | 在乾淨環境執行安裝包。 | 環境清單與健康檢查回應。 | 已通過 |
| B-04 | 要求包含唯一識別碼、操作與資料。 | 傳送兩筆連續要求。 | 請求／回應紀錄。 | 已通過 |
| B-05 | 回應可傳回結果、警告與結構化錯誤。 | 執行成功與故意失敗的要求。 | 錯誤與成功紀錄。 | 已通過 |
| B-06 | 長作業會傳回可見進度。 | 執行大型檔案或批次標籤作業。 | 至少兩次中間進度與最終結果。 | 已通過 |
| B-07 | 進度透過 `core://progress` 事件回傳。 | 檢查前端監聽與 Rust 事件。 | 事件紀錄。 | 已通過 |
| B-08 | 發行包不包含外部核心執行檔或 taglib WASM。 | 檢查 AppImage／DEB 資源。 | 安裝包清單。 | 已通過 |
| B-09 | 前端無法執行任意 Shell 命令。 | 檢查 capability 與前端呼叫。 | 權限清單。 | 已通過 |

### C. 文字轉換引擎

| 編號 | 驗收條件 | 判定方式 | 必要證據 | 目前狀態 |
| --- | --- | --- | --- | --- |
| C-01 | 預設引擎為 `segmented`。 | 清除設定後啟動程式。 | 畫面截圖與設定內容。 | 已通過 |
| C-02 | 新式引擎使用 `ws-segment-rs` 與 `cjk-convert-rs`。 | 檢查鎖定版本與執行路徑。 | 相依清單與測試紀錄。 | 已通過 |
| C-03 | 簡轉繁會進行分詞、同義詞最佳化與地區字形轉換。 | 執行黃金測試。 | 測試結果。 | 已通過 |
| C-04 | 繁轉簡會先分詞再進行字形轉換。 | 執行黃金測試。 | 測試結果。 | 已通過 |
| C-05 | 「裡面」、「皇后」、「頭髮」與「開發」符合預期。 | 執行四組雙向黃金測試。 | 測試案例與結果。 | 已通過 |
| C-06 | 空白、換行、標點與未知片段保持原位。 | 比較輸入與輸出位置。 | 測試案例與結果。 | 已通過 |
| C-07 | 長文字會在安全邊界分段。 | 轉換超過單段上限的文字。 | 分段測試結果。 | 已通過 |
| C-08 | 舊方法可讀取六欄 `Dictionary.csv`。 | 載入含 BOM 的舊字典。 | 測試結果。 | 已通過 |
| C-09 | 舊方法保留啟用欄位、優先權與長詞優先。 | 執行衝突詞條測試。 | 測試結果。 | 已通過 |
| C-10 | 優先權 `9999` 會保護指定詞。 | 執行保護詞測試。 | 測試結果。 | 已通過 |
| C-11 | 舊方法未命中字元交由 `cjk-convert-rs`。 | 使用不在字典內的字元。 | 測試結果。 | 已通過 |
| C-12 | ZhConvert 呼叫官方 `/convert` 端點。 | 使用模擬伺服器核對請求。 | 測試紀錄。 | 已通過 |
| C-13 | `/service-info` 快取二十四小時。 | 連續執行兩次轉換。 | 模擬伺服器請求次數。 | 已通過 |
| C-14 | ZhConvert 依服務大小限制切分文字。 | 回傳小型限制後轉換長文字。 | 請求分段紀錄。 | 已通過 |
| C-15 | ZhConvert 網路失敗時不切換引擎。 | 模擬連線失敗。 | 結構化錯誤紀錄。 | 已通過 |
| C-16 | ZhConvert 畫面顯示署名與商業使用提醒。 | 開啟 ZhConvert 選項。 | 畫面截圖。 | 已通過 |

### D. 編碼、文字工具與相容資料（已通過子集）

| 編號 | 驗收條件 | 判定方式 | 必要證據 | 目前狀態 |
| --- | --- | --- | --- | --- |
| D-01 | UTF-8 與 UTF-16 可正確讀寫。 | 執行編碼往返測試。 | 測試結果。 | 已通過 |
| D-02 | Big5、GBK 與 Shift-JIS 可正確讀寫。 | 執行編碼往返測試。 | 測試結果。 | 已通過 |
| D-03 | EUC-JP、ISO-2022-JP 與 HZ-GB-2312 可正確讀寫。 | 執行編碼往返測試。 | 測試結果。 | 已通過 |
| D-04 | HTML 十進位與十六進位工具可雙向處理。 | 執行工具測試與畫面操作。 | 測試結果與截圖。 | 已通過 |
| D-05 | 全形與半形工具可雙向處理。 | 執行工具測試與畫面操作。 | 測試結果與截圖。 | 已通過 |
| D-06 | Unicode 跳脫工具保留舊版能力。 | 執行編碼與解碼案例。 | 測試結果與截圖。 | 已通過 |
| D-08 | 匯入只讀取舊 `ConvertZZ.json`，結果另存為 2.0 設定。 | 匯入前後比對來源。 | 來源雜湊不變，目錄沒有備份檔。 | 已通過 |
| D-09 | 讀取或轉換失敗時不覆寫目前設定，也不修改來源。 | 對不存在或無法讀取的檔案執行匯入。 | 測試結果與錯誤畫面。 | 已通過 |
| D-07 | 首次匯入 `ConvertZZ.json` 前會詢問使用者。 | 使用含舊設定的乾淨資料目錄啟動。 | 操作錄影。 | 已通過 |
| D-10 | 所有已匯入的 `SettingsV2` 欄位會影響實際行為。 | 逐欄變更並重啟驗證。 | 設定驗收表。 | 已通過 |
| D-11 | 儲存舊字典前會詢問使用者。 | 修改一筆詞條後按下儲存。 | 操作錄影。 | 已通過 |
| D-12 | 同意儲存後會先建立不覆蓋的時間戳備份。 | 連續儲存兩次。 | 備份檔名與內容雜湊。 | 已通過 |

### E. 檔案與檔名安全（已通過子集）

| 編號 | 驗收條件 | 判定方式 | 必要證據 | 目前狀態 |
| --- | --- | --- | --- | --- |
| E-01 | 檔案內容作業會先顯示來源、輸出、編碼與差異。 | 建立檔案轉換計畫。 | 預覽截圖。 | 已通過 |
| E-02 | 檔名作業會先顯示來源與輸出。 | 建立批次重新命名計畫。 | 預覽截圖。 | 已通過 |
| E-03 | 未確認前不會寫入或重新命名。 | 在預覽畫面取消。 | 前後檔案雜湊與目錄清單。 | 已通過 |
| E-04 | 衝突策略預設為略過。 | 建立同名輸出。 | 結果與原檔雜湊。 | 已通過 |
| E-05 | 覆寫需要額外確認。 | 選取覆寫策略。 | 操作錄影。 | 已通過 |
| E-06 | 內容寫入使用同目錄暫存檔。 | 監看作業目錄。 | 檔案事件紀錄。 | 已通過 |
| E-07 | 驗證成功後才會原子取代。 | 模擬寫入成功與驗證失敗。 | 測試結果與檔案雜湊。 | 已通過 |
| E-08 | 檔名使用兩階段重新命名。 | 建立名稱互換案例。 | 測試結果與目錄清單。 | 已通過 |
| E-09 | 中途失敗時會回復原始名稱。 | 在第二階段注入失敗。 | 測試結果與目錄清單。 | 已通過 |
| E-10 | 遞迴處理不跟隨符號連結。 | 建立指向外部目錄的連結。 | 測試結果與外部目錄雜湊。 | 已通過 |
| E-11 | 唯讀檔案會回報可辨識錯誤。 | 對唯讀檔案執行寫入。 | 結構化錯誤紀錄。 | 已通過 |

### F. 音訊標籤

| 編號 | 驗收條件 | 判定方式 | 必要證據 | 目前狀態 |
| --- | --- | --- | --- | --- |
| F-01 | MP3 使用 Rust `id3` 讀寫 ID3v1 與 ID3v2。 | 掃描並更新 MP3 樣本。 | 測試結果與標籤清單。 | 已通過 |
| F-02 | ID3v1 可修復 Big5 與 GBK 文字。 | 使用兩種損壞標籤樣本。 | 修復前後欄位值。 | 已通過 |
| F-03 | ID3v2.3、ID3v2.4 與文字編碼選項可用。 | 逐一寫入並重新讀取。 | 標籤版本與內容紀錄。 | 已通過 |
| F-04 | APE 使用 Rust `lofty` 讀寫 APEv2。 | 掃描並更新 APE 樣本。 | 測試結果與標籤清單。 | 已通過 |
| F-05 | OGG 與 OGA 使用 Vorbis Comment。 | 掃描並更新兩種樣本。 | 測試結果與標籤清單。 | 已通過 |
| F-06 | Opus 使用 Vorbis Comment。 | 掃描並更新 Opus 樣本。 | 測試結果與標籤清單。 | 已通過 |
| F-07 | APEv2 與 Vorbis Comment 不顯示無效編碼選項。 | 開啟 APE、OGG 與 Opus 預覽。 | 畫面截圖。 | 已通過 |
| F-08 | 所有可轉換字串欄位都會顯示。 | 使用標準、自訂與多值欄位樣本。 | 預覽截圖與原始標籤清單。 | 已通過 |
| F-09 | 多值欄位會逐值轉換。 | 使用含兩個以上值的欄位。 | 轉換前後欄位值。 | 已通過 |
| F-10 | 未選欄位保持不變。 | 取消一個欄位後執行。 | 前後欄位雜湊。 | 已通過 |
| F-11 | 未知欄位保持不變。 | 使用自訂未知欄位樣本。 | 前後欄位值。 | 已通過 |
| F-12 | 二進位欄位與封面圖片保持不變。 | 更新含封面的樣本。 | 前後二進位雜湊。 | 已通過 |
| F-13 | 音訊內容不會重新編碼。 | 比較轉換前後的解碼後音訊雜湊。 | 雜湊紀錄。 | 已通過 |
| F-14 | 損毀與截斷檔案回報可辨識錯誤。 | 掃描兩種錯誤樣本。 | 結構化錯誤紀錄。 | 已通過 |
| F-15 | 安裝包可離線掃描 APE 與 OGG，且不含 taglib WASM。 | 在離線環境掃描 APE 與 OGG。 | 安裝包清單與操作錄影。 | 已通過 |

### G. 桌面介面與作業系統整合（已通過子集）

| 編號 | 驗收條件 | 判定方式 | 必要證據 | 目前狀態 |
| --- | --- | --- | --- | --- |
| G-01 | 主視窗含八個計畫頁面。 | 逐頁開啟並檢查。 | 頁面截圖組。 | 已通過 |
| G-02 | 浮動球使用獨立透明視窗。 | 顯示浮動球。 | Windows 與 Linux 截圖。 | 已通過 |
| G-03 | 浮動球可用滑鼠拖動。 | 拖動後關閉並重啟。 | 操作錄影。 | 已通過 |
| G-04 | 浮動球位置會在重啟後復原。 | 記錄拖動前後座標。 | 操作錄影與設定內容。 | 已通過 |
| G-05 | 浮動球圖示沒有白底外框。 | 在深色與淺色桌面檢查。 | 兩張截圖。 | 已通過 |
| G-06 | 浮動球顯示一大一小的 Z。 | 目視檢查圖示。 | 截圖。 | 已通過 |
| G-07 | 系統托盤會顯示應用程式圖示。 | 啟動後檢查通知區。 | Windows 與 Linux 截圖。 | 已通過 |
| G-08 | Windows 托盤左鍵會顯示主視窗。Linux 會提供等效的開啟選單。 | 隱藏主視窗後操作托盤。 | Windows 與 Linux 操作錄影。 | 已通過 |
| G-09 | 托盤右鍵會顯示選單。 | 右鍵點擊托盤。 | 操作錄影。 | 已通過 |
| G-10 | 關閉主視窗時程式會留在托盤。 | 點擊主視窗關閉按鈕。 | 操作錄影與程序清單。 | 已通過 |
| G-11 | 剪貼簿讀寫不會出現 capability 錯誤。 | 執行浮動球左右鍵轉換。 | 操作錄影與錯誤紀錄。 | 已通過 |
| G-12 | 快捷鍵、托盤與浮動球呼叫相同動作路由。 | 對相同剪貼簿文字執行三種入口。 | 三次輸出比對。 | 已通過 |
| G-13 | 第二個程序會把參數交給既有視窗。 | 連續啟動兩次程式。 | 操作錄影與程序清單。 | 已通過 |
| G-14 | 問題回報會開啟 GitHub Issues。 | 點擊問題回報。 | 瀏覽器網址截圖。 | 已通過 |

### H. 舊版命令列相容

| 編號 | 驗收條件 | 判定方式 | 必要證據 | 目前狀態 |
| --- | --- | --- | --- | --- |
| H-01 | `/e:l`、`/e:f` 與 `/e:n` 對應三種引擎。 | 解析三組參數。 | 測試結果。 | 已通過 |
| H-02 | `/audio` 會依副檔名辨識 ID3、APEv2 與 Vorbis Comment。 | 傳入四種音訊檔案。 | 預覽結果。 | 已通過 |
| H-03 | `/file` 會建立檔案預覽。 | 使用命令列開啟文字檔。 | 畫面截圖。 | 已通過 |
| H-04 | 舊版方向、詞彙修正與編碼參數保持相容。 | 執行參數矩陣測試。 | 測試結果。 | 已通過 |
| H-05 | 舊版第二輸出檔與萬用字元語意保持相容。 | 執行代表性檔名案例。 | 測試結果與輸出清單。 | 已通過 |
| H-06 | Windows SendTo 捷徑只出現在 Windows。 | 在 Windows 啟用與停用。 | 捷徑內容與操作錄影。 | 已通過 |

### I. 平台差異（已通過子集）

| 編號 | 驗收條件 | 判定方式 | 必要證據 | 目前狀態 |
| --- | --- | --- | --- | --- |
| I-01 | Windows 可使用全域快捷鍵與自動複製貼上。 | 在 Windows x64 實機操作。 | 操作錄影。 | 已通過 |
| I-02 | Linux X11 可使用全域快捷鍵與自動複製貼上。 | 在 X11 實機操作。 | 操作錄影。 | 已通過 |
| I-09 | Linux 使用者不需要安裝 AppIndicator 開發套件。 | 在沒有 `libayatana-appindicator3-dev` 的乾淨環境執行安裝包。 | 套件清單與操作錄影。 | 已通過 |

### J. 測試與發行（已通過子集）

| 編號 | 驗收條件 | 判定方式 | 必要證據 | 目前狀態 |
| --- | --- | --- | --- | --- |
| J-01 | `pnpm run check` 全部必要測試通過。 | 在含完整音訊樣本的 CI 執行測試。 | CI 連結。 | 已通過 |
| J-02 | 音訊測試包含 MP3、APE、OGG 與 Opus。 | 檢查測試樣本與完整報告。 | 無略過項目的測試報告。 | 已通過 |
| J-03 | ZhConvert 測試不連線至正式服務。 | 檢查測試網路目標。 | 模擬伺服器紀錄。 | 已通過 |
| J-04 | Windows x64 產生 NSIS 與 MSI。 | 執行 Release 工作流程。 | 兩個可下載產物。 | 已通過 |
| J-05 | Linux x64 產生 AppImage、DEB 與 RPM。 | 在 Ubuntu 22.04 執行工作流程。 | 三個可下載產物。 | 已通過 |
| J-07 | Linux 執行相依不含 Node.js、`*-dev` 與其他建置套件。 | 檢查 DEB、RPM 與相依報告。 | 相依報告。 | 已通過 |
| J-09 | Linux 乾淨環境不需 Node.js 即可安裝與執行。 | 使用全新 Ubuntu 22.04 虛擬機。 | 環境清單與操作錄影。 | 已通過 |
| J-10 | AppImage 不要求使用者安裝開發套件。 | 在未安裝 `*-dev` 套件的環境執行。 | 套件清單與操作錄影。 | 已通過 |
| J-13 | 發行包不會在執行時下載 WASM。 | 離線執行 APE 與 OGG 掃描。 | 網路監看與操作錄影。 | 已通過 |

### K. 最終退場閘門（已通過子集）

| 編號 | 驗收條件 | 判定方式 | 必要證據 | 目前狀態 |
| --- | --- | --- | --- | --- |
| K-02 | 所有已知平台限制已寫入 README 差異表。 | 比對失敗與限制紀錄。 | README 差異。 | 已通過 |

